1. npm install

3. tsconfig.json

4. npx prisma init
   prisma format
   npx prisma migrate dev
   npx prisma generate

4. package.json scripts to run

5. npm run init-db
   npm run dev

6. Current issues/ missing:
   - Optimization
   - Front
   - Testing

7. Before deploying
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "init-db": "ts-node src/scripts/dbInitializer.ts",
    "migrate": "prisma migrate dev"
  }